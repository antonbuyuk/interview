# Frontend Interview Cheatsheet

Full-stack приложение для подготовки к собеседованиям на позицию Frontend Developer с админ-панелью, базой данных и расширенными функциями для изучения.

**Сайт доступен по адресу:** https://antonbuyuk.github.io/interview/

## 🚀 Технологии

### Frontend
- **Vue 3** - фреймворк с Composition API и TypeScript
- **Pinia** - state management
- **Vue Router** - клиентский роутинг
- **Vite** - быстрый сборщик и dev-сервер
- **Marked** - парсинг Markdown
- **Highlight.js** - подсветка синтаксиса кода
- **TipTap** - rich text редактор для админ-панели
- **Sass** - препроцессор стилей

### Backend
- **Express** - Node.js веб-фреймворк
- **TypeScript** - типизированный JavaScript
- **Prisma ORM** - работа с базой данных
- **PostgreSQL** - реляционная база данных
- **JWT** - аутентификация администратора
- **Swagger/OpenAPI** - документация API

### Инфраструктура и инструменты
- **GitHub Actions** - CI/CD для деплоя
- **Railway/Render** - хостинг API сервера
- **Supabase/Neon** - облачная PostgreSQL БД
- **Sentry** - мониторинг ошибок
- **Vitest** - unit тестирование
- **Playwright** - E2E тестирование

## 📦 Установка

```bash
npm install
```

Установка автоматически выполнит `prisma generate` для генерации Prisma Client.

## 🛠 Разработка

### Локальная разработка (только фронтенд)

```bash
npm run dev
```

Фронтенд будет доступен по адресу `http://localhost:3000` и автоматически откроется в браузере.

### Локальная разработка (фронтенд + API сервер)

Для полной разработки с API и базой данных:

1. Создайте файл `.env` в корне проекта:

```env
# База данных
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"

# API сервер
API_PORT=3001
NODE_ENV=development

# Frontend
VITE_API_URL=http://localhost:3001

# Админ-панель (опционально)
ADMIN_PASSWORD=your-secure-password
JWT_SECRET=your-jwt-secret

# CORS (опционально)
CORS_ORIGINS=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Sentry (опционально)
SENTRY_DSN=your-sentry-dsn
```

2. Выполните миграции базы данных:

```bash
npm run prisma:migrate
```

3. Заполните базу данных (если необходимо):

```bash
npm run migrate:data
```

4. Запустите фронтенд и API одновременно:

```bash
npm run dev:all
```

Или в отдельных терминалах:

```bash
# Терминал 1: API сервер
npm run dev:server

# Терминал 2: Frontend
npm run dev:client
```

API сервер будет доступен на `http://localhost:3001`, фронтенд на `http://localhost:3000`.

### Работа с базой данных

```bash
# Генерация Prisma Client (выполняется автоматически при установке)
npm run prisma:generate

# Создание новой миграции
npm run prisma:migrate

# Применение миграций (для продакшена)
npm run prisma:migrate:deploy

# Открыть Prisma Studio (GUI для БД)
npm run prisma:studio
```

## 📁 Структура проекта

```
├── server/                       # 🔧 Backend API
│   ├── controllers/              # Контроллеры для обработки запросов
│   │   ├── adminController.ts    # Админ-панель
│   │   ├── answersController.ts  # Ответы на вопросы
│   │   ├── questionsController.ts # Вопросы
│   │   ├── sectionsController.ts # Разделы
│   │   └── termsController.ts    # Словарь терминов
│   ├── middleware/               # Express middleware
│   │   ├── authAdmin.ts          # Аутентификация админа
│   │   ├── errorHandler.ts       # Обработка ошибок
│   │   ├── performance.ts        # Мониторинг производительности
│   │   └── rateLimiter.ts        # Ограничение частоты запросов
│   ├── routes/                   # API роуты
│   ├── types/                    # TypeScript типы
│   ├── utils/                    # Утилиты
│   │   ├── cache.ts              # Кэширование
│   │   ├── jwt.ts                # JWT токены
│   │   ├── logger.ts             # Логирование
│   │   ├── prisma.ts             # Prisma клиент
│   │   └── sentry.ts             # Sentry интеграция
│   └── index.ts                  # Точка входа API сервера
├── src/                          # 💻 Frontend приложение
│   ├── api/                      # API клиенты
│   ├── components/               # Vue компоненты
│   │   ├── content/              # Компоненты контента
│   │   ├── modals/               # Модальные окна
│   │   ├── question/             # Компоненты вопросов
│   │   ├── ui/                   # UI компоненты
│   │   └── vocabulary/           # Компоненты словаря
│   ├── composables/              # Vue composables
│   │   ├── useAdminAuth.ts       # Аутентификация админа
│   │   ├── useDictionaryHighlight.ts # Подсветка терминов
│   │   ├── useTheme.ts           # Темная/светлая тема
│   │   ├── useTrainingMode.ts    # Режим тренировки
│   │   └── ...
│   ├── router/                   # Vue Router конфигурация
│   ├── stores/                   # Pinia stores
│   ├── styles/                   # SCSS стили
│   ├── types/                    # TypeScript типы
│   ├── utils/                    # Утилиты
│   ├── views/                    # Страницы
│   │   ├── HomeView.vue          # Главная страница
│   │   ├── SectionView.vue       # Страница раздела
│   │   └── VocabularyView.vue    # Словарь терминов
│   ├── App.vue                   # Главный компонент
│   └── main.ts                   # Точка входа
├── prisma/                       # 🗄 База данных
│   └── schema.prisma             # Prisma схема
├── tests/                        # 🧪 Тесты
│   ├── api/                      # API тесты
│   ├── components/               # Компонентные тесты
│   ├── composables/              # Тесты composables
│   └── e2e/                      # E2E тесты
├── scripts/                      # 🔧 Скрипты
│   ├── migrate-to-db.js          # Миграция данных в БД
│   ├── translate-questions.js    # Перевод вопросов
│   └── ...
├── public/                       # 📦 Статические файлы
│   └── sw.js                     # Service Worker для офлайн
├── .github/workflows/            # GitHub Actions
│   └── deploy.yml                # Автодеплой на GitHub Pages
├── Dockerfile                    # Docker образ для API
├── render.yaml                   # Конфигурация Render
├── railway.json                  # Конфигурация Railway
├── package.json
└── vite.config.ts
```

## ✨ Особенности

### Для пользователей
- 📱 **Адаптивный дизайн** - работает на всех устройствах
- 🎨 **Темная/светлая тема** - переключение между темами
- 📝 **Подсветка синтаксиса** - автоматическая подсветка кода в markdown
- 🔍 **Удобная навигация** - боковое меню со всеми разделами
- ⚡ **Быстрая загрузка** - благодаря Vite и оптимизациям
- 📚 **Словарь терминов** - интерактивные подсказки при наведении на термины
- 🎯 **Режим тренировки** - скрытие ответов для самостоятельной подготовки
- 💾 **Офлайн поддержка** - работа без интернета через Service Worker
- ⌨️ **Горячие клавиши** - быстрая навигация с клавиатуры
- 🔊 **Text-to-Speech** - озвучивание вопросов и ответов

### Для администраторов
- 🔐 **Админ-панель** - управление контентом через веб-интерфейс
- ➕ **Добавление вопросов** - создание новых вопросов с форматированием
- 📝 **Редактирование контента** - изменение разделов, вопросов и ответов
- 📖 **Управление словарем** - добавление терминов с примерами и фразами
- 🌐 **Мультиязычность** - поддержка русского и английского языков
- 🔄 **Автоматическое сохранение** - изменения сохраняются в базу данных

## 🎨 Интерфейс

- **Главная страница** - отображает все разделы в виде карточек с количеством вопросов
- **Страница раздела** - показывает вопросы с ответами, навигацией и функционалом админа
- **Словарь терминов** - просмотр всех терминов с примерами использования
- **Боковое меню** - всегда доступная навигация по разделам
- **Вторичное меню** - доступ к настройкам, словарю, режиму тренировки

## 🔐 Админ-панель

Для доступа к админ-панели:

1. Установите переменную окружения `ADMIN_PASSWORD` на сервере
2. Откройте приложение и нажмите кнопку "Войти" в меню
3. Введите пароль администратора

Функции админ-панели:
- Управление разделами (создание, редактирование, удаление)
- Управление вопросами (добавление, редактирование, удаление)
- Управление словарем (добавление терминов, примеров, фраз)
- Переключение между ответами на русском и английском языках

API документация доступна по адресу `/api-docs` (Swagger UI).

## 🧪 Тестирование

```bash
# Запуск всех тестов
npm test

# Unit тесты с UI
npm run test:ui

# Тесты с покрытием
npm run test:coverage

# E2E тесты
npm run test:e2e

# E2E тесты с UI
npm run test:e2e:ui

# E2E тесты в headed режиме (с браузером)
npm run test:e2e:headed
```

## 🔧 Дополнительные команды

```bash
# Проверка типов TypeScript
npm run type-check

# Линтинг кода
npm run lint

# Автоисправление ошибок линтера
npm run lint:fix

# Форматирование кода
npm run format

# Проверка форматирования
npm run format:check

# Все проверки и исправления
npm run fix

# Предпросмотр production сборки
npm run preview

# Анализ размера bundle
npm run build:analyze
```

## 🌐 Деплой

Проект состоит из трех компонентов:

1. **Frontend** (Vue 3) - деплоится на GitHub Pages
2. **API Server** (Express) - деплоится на Railway или Render
3. **Database** (PostgreSQL) - использует Supabase или Neon

Подробные инструкции по деплою см. в [DEPLOY.md](DEPLOY.md).

### Быстрый старт

1. Настройте базу данных (Supabase/Neon)
2. Задеплойте API на Railway/Render
3. Настройте переменные окружения
4. Задеплойте фронтенд на GitHub Pages

При каждом пуше в ветку `main` фронтенд автоматически деплоится через GitHub Actions.

## 📝 Переменные окружения

### API Server

| Переменная | Описание | Обязательно | По умолчанию |
|------------|----------|-------------|--------------|
| `DATABASE_URL` | Строка подключения к PostgreSQL | ✅ Да | - |
| `NODE_ENV` | Окружение (development/production) | ❌ Нет | development |
| `PORT` / `API_PORT` | Порт API сервера | ❌ Нет | 3001 |
| `ADMIN_PASSWORD` | Пароль администратора | ❌ Нет | - |
| `JWT_SECRET` | Секрет для JWT токенов | ❌ Нет | default-secret |
| `CORS_ORIGINS` | Разрешенные CORS origins (через запятую) | ❌ Нет | - |
| `FRONTEND_URL` | URL фронтенда для CORS | ❌ Нет | - |
| `SENTRY_DSN` | DSN для Sentry мониторинга | ❌ Нет | - |
| `DISABLE_SWAGGER` | Отключить Swagger UI | ❌ Нет | false |

### Frontend

| Переменная | Описание | Обязательно | По умолчанию |
|------------|----------|-------------|--------------|
| `VITE_API_URL` | URL API сервера | ✅ Да | http://localhost:3001 |

Переменные фронтенда должны начинаться с префикса `VITE_` для доступности в клиентском коде.

## 🔄 Миграция данных

Если нужно перенести данные из markdown файлов в базу данных:

```bash
npm run migrate:data
```

Скрипт `scripts/migrate-to-db.js` читает данные и заполняет базу данных.

## 📚 API

API документация доступна в Swagger UI по адресу `/api-docs` на API сервере.

Основные endpoints:
- `GET /api/sections` - список разделов
- `GET /api/sections/:id/questions` - вопросы раздела
- `GET /api/questions/:id` - конкретный вопрос
- `GET /api/answers/:questionId` - ответы на вопрос
- `GET /api/terms` - список терминов
- `POST /api/admin/login` - авторизация администратора
- `POST /api/admin/*` - защищенные admin endpoints

## 🤝 Разработка

### Требования

- Node.js >= 22.12.0
- npm >= 10.0.0
- PostgreSQL (локально или через Supabase/Neon)

### Правила разработки

- Используйте TypeScript для всего кода
- Следуйте правилам ESLint и Prettier
- Пишите тесты для новой функциональности
- Используйте Composition API для Vue компонентов
- Добавьте типы для всех API запросов

## 📄 Лицензия

MIT
