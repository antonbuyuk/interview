# Frontend Interview Cheatsheet SPA

SPA приложение на Vue 3 для удобного просмотра вопросов для собеседования Frontend Developer.

## 🚀 Технологии

- **Vue 3** - последняя версия Vue.js (Composition API)
- **Vite** - быстрый сборщик и dev-сервер
- **Vue Router** - роутинг для навигации между разделами
- **Marked** - парсинг Markdown файлов
- **Highlight.js** - подсветка синтаксиса кода

## 📦 Установка

```bash
npm install
```

## 🛠 Разработка

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000` и автоматически откроется в браузере.

При первом запуске скрипт автоматически скопирует все README.md файлы из папки `content/` в `public/`.

## 🌐 Деплой на GitHub Pages

Проект настроен для автоматического деплоя на GitHub Pages.

**Сайт доступен по адресу:** https://antonbuyuk.github.io/interview/

### Автоматический деплой

При каждом пуше в ветку `main` автоматически запускается деплой через GitHub Actions.

### Первоначальная настройка

1. Перейдите в настройки репозитория: **Settings → Pages**
2. В разделе "Source" выберите **"GitHub Actions"**
3. Сохраните изменения

Подробнее см. [DEPLOY.md](DEPLOY.md)

## 🏗 Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в директории `dist/`.

## 📁 Структура проекта

```
├── content/                    # 📚 MD документы с вопросами
│   ├── javascript-typescript/
│   │   └── README.md
│   ├── vuejs/
│   │   └── README.md
│   ├── nuxtjs/
│   │   └── README.md
│   └── ... (остальные разделы)
├── src/                        # 💻 Исходный код приложения
│   ├── components/             # Vue компоненты
│   │   └── Sidebar.vue         # Боковое меню навигации
│   ├── views/                  # Страницы приложения
│   │   ├── HomeView.vue        # Главная страница
│   │   └── SectionView.vue     # Страница раздела
│   ├── data/                   # Данные
│   │   └── sections.js         # Список всех разделов
│   ├── routes.js               # Конфигурация Vue Router
│   ├── App.vue                 # Главный компонент
│   └── main.js                 # Точка входа
├── scripts/                    # 🔧 Скрипты
│   └── copy-markdown.js        # Копирование MD файлов
├── public/                     # 📦 Статические файлы
│   └── ... (скопированные README.md)
├── package.json
└── vite.config.js
```

## ✨ Особенности

- 📱 **Адаптивный дизайн** - работает на всех устройствах
- 🎨 **Современный UI** - темная боковая панель, светлый контент
- 📝 **Подсветка синтаксиса** - автоматическая подсветка кода в markdown
- 🔍 **Удобная навигация** - боковое меню со всеми разделами
- ⚡ **Быстрая загрузка** - благодаря Vite и оптимизациям
- 🎯 **Роутинг** - каждая директория имеет свой URL

## 🎨 Интерфейс

- **Главная страница** - отображает все разделы в виде карточек
- **Страница раздела** - показывает содержимое README.md с форматированием
- **Боковое меню** - всегда доступная навигация по всем разделам

## 📝 Работа с контентом

### Добавление новых вопросов

1. Найдите нужный раздел в папке `content/`
2. Отредактируйте файл `README.md` в соответствующей директории
3. Перезапустите dev-сервер (или запустите `npm run copy`)

### Структура контента

Все markdown документы находятся в папке `content/`, каждая подпапка представляет отдельный раздел:

- `content/javascript-typescript/README.md` - вопросы по JavaScript/TypeScript
- `content/vuejs/README.md` - вопросы по Vue.js
- `content/nuxtjs/README.md` - вопросы по Nuxt.js
- ... и так далее

## 🔄 Обновление контента

Если вы обновили README.md файлы в папке `content/`:

1. Остановите dev-сервер (Ctrl+C)
2. Запустите снова: `npm run dev`

Или запустите скрипт копирования отдельно:
```bash
npm run copy
```
