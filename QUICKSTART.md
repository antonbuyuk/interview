# 🚀 Быстрый старт

## 1. Установка зависимостей

```bash
npm install
```

## 2. Запуск приложения

```bash
npm run dev
```

Приложение автоматически:
- Скопирует все README.md файлы из `content/` в `public/`
- Запустит dev-сервер на `http://localhost:3000`
- Откроется в браузере

## 3. Что дальше?

- На главной странице вы увидите все разделы в виде карточек
- В боковом меню доступна навигация по всем разделам
- Каждый раздел отображает содержимое своего README.md файла

## 📝 Структура URL

- `/` - Главная страница
- `/javascript-typescript` - JavaScript / TypeScript
- `/vuejs` - Vue.js
- `/nuxtjs` - Nuxt.js
- `/typescript` - TypeScript
- ... и так далее для всех разделов

## 📁 Структура контента

Все markdown файлы находятся в папке `content/`:

```
content/
├── javascript-typescript/README.md
├── vuejs/README.md
├── nuxtjs/README.md
└── ... (остальные разделы)
```

## 🔄 Обновление контента

Если вы обновили README.md файлы в `content/`:
1. Остановите dev-сервер (Ctrl+C)
2. Запустите снова: `npm run dev`

Или запустите скрипт копирования отдельно:
```bash
npm run copy
```
