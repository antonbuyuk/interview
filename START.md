# 🚀 Инструкция по запуску

## Шаг 1: Установка зависимостей

Откройте терминал в корне проекта и выполните:

```bash
npm install
```

Это установит все необходимые пакеты (Vue, Vite, Vue Router и т.д.)

## Шаг 2: Запуск приложения

После установки зависимостей запустите dev-сервер:

```bash
npm run dev
```

## Что произойдет:

1. ✅ Скрипт автоматически скопирует все README.md из `content/` в `public/`
2. ✅ Запустится dev-сервер Vite
3. ✅ Приложение откроется автоматически в браузере на `http://localhost:3000`

## Дополнительные команды:

```bash
# Только скопировать файлы (без запуска сервера)
npm run copy

# Сборка для продакшена
npm run build

# Просмотр собранной версии
npm run preview
```

## ❓ Проблемы?

Если что-то не работает:
- Убедитесь, что у вас установлен Node.js (версия 16+)
- Проверьте, что вы находитесь в правильной директории
- Попробуйте удалить `node_modules` и `package-lock.json`, затем `npm install` снова

